{
  "name": "photo-excel-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:web": "node -e \"console.log('Web assets are already in www/')\"",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0",
    "@capacitor/filesystem": "^7.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}